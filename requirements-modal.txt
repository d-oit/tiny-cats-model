# Modal container image dependencies
# Pinned versions for reproducible builds (ADR-022)
# Use with: .uv_pip_install_from_requirements("requirements-modal.txt")

# Core ML dependencies
torch==2.12.1
torchvision==0.27.0

# Image processing
pillow==12.2.0

# Progress bars
tqdm==4.67.3

# Modal SDK (for local development)
modal==1.4.3

# HuggingFace Hub integration (ADR-026)
huggingface_hub==1.15.0
safetensors==0.7.0

# Experiment tracking (ADR-027)
mlflow==3.12.0

# ONNX export support (ADR-022)
onnx==1.21.0
onnxruntime==1.23.2
